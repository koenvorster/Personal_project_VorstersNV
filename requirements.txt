# VorstersNV – Python afhankelijkheden

# Web framework & ASGI server
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1

# Data validatie
pydantic==2.10.4
pydantic-settings==2.7.0

# YAML verwerking
pyyaml==6.0.2

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Cache
redis==5.2.1

# Testen
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx  # al hierboven

# Auth & Beveiliging
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Code kwaliteit
ruff==0.8.6
mypy==1.14.0

# Alembic sync driver (voor migraties - asyncpg is voor runtime)
psycopg2-binary==2.9.10

# Test database (in-memory SQLite voor unit tests)
aiosqlite==0.20.0

# IT/AI Consultancy Wave 6
tiktoken==0.8.0                    # token counting (optioneel, fallback naar schatting)
sentence-transformers==3.3.1       # text embeddings voor RAG (Wave 7)
pgvector==0.3.6                    # PostgreSQL vector extensie driver (Wave 7)
presidio-analyzer==2.2.355         # PII detectie (Wave 6)
presidio-anonymizer==2.2.355       # PII anonimisering (Wave 6)
