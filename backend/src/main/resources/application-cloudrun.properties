# Cloud Run profiel – verbinding via Cloud SQL Unix socket
spring.datasource.url=jdbc:postgresql:///vorstersNV?socketFactory=com.google.cloud.sql.postgres.SocketFactory&cloudSqlInstance=${CLOUD_SQL_INSTANCE}
spring.datasource.username=${DB_USER:vorstersNV}
spring.datasource.password=${DB_PASSWORD}
